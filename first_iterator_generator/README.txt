«адание 1.



Ќаписать базовый класс »тератора, от которого можно будет наследовавтьс€ и реализовывать свои »тераторные объекты дл€ работы с списками, кортежами и множествами.



“ребовани€:

¬ результате должно быть 3-4 класса (базовый + наследники дл€ каждого типа), по которым можно будет пробегать в цикле, и брать по индексу элемент (кроме можеств).
 аждый из наследников должен реализовывать роботу со своим типом
 ” каждого класа должно быть читаемое представление при выводе (repr)
 аждый класс должен быть максимально оптимизированным и использовать лучшие практики, которые проходили до этого.


«адание 2.



–асшир€ем наши классы.



” каждого класа должен быть публичный метод, который будет генерировать объект-последовательность (дл€ каждого класса свой тип, не использу€ встроенный range) и возвращать его по заданным аргументам.



“ребовани€:

ћетод должен возвращать генераторный объект своего типа
ћетод должен принимать три аргумента - старт, финиш и шаг генерируемой последовательности (по аналогии с прошлым домашним заданием, только аргументы передаютьс€ в этот метод, а не при инициализации класса)