Ќаписать класс, суть которого будет в поиске максимального простого числа в заданном массиве целых чисел.
 ласс должен принимать при инициализации максимальное и минимальное значение (в рамках которых будет производитьс€ поиск) + третий (необ€зательный) параметр с шагом между каждым новым числомв последовательности (по умолчанию 1).

“ребовани€:

 ласс не должен использовать стандартную функцию range() (ее реализовать стоит самому)
 ласс должен иметь два публичных метода: поиск и получение сгенерированного массива чисел.
ћинимальным и максимальным аргементом могут быть и отрицательные числа.
¬ случае невалидных аргументов (типы, значение min > max) класс должен выдавать свою специальную ошибку (исключение).
repr экземпл€ра класса должен возвращать читаемое его текущее состо€ние (Ќазвание класса + минимальное/максимальное значение + шаг)